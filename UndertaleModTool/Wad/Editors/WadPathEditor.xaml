<local:DataUserControl x:Class="UndertaleModTool.WadPathEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UndertaleModTool"
             xmlns:wad="clr-namespace:UndertaleModLib.Wad;assembly=UndertaleModLib"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance wad:WadPathEntry}">
    <DockPanel Margin="12">
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Margin="3" FontWeight="Bold"/>
            <local:TextBoxDark Grid.Row="0" Grid.Column="1" Margin="3"
                               Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Kind" Margin="3"/>
            <local:TextBoxDark Grid.Row="1" Grid.Column="1" Margin="3" Text="{Binding Kind, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Closed" Margin="3"/>
            <CheckBox Grid.Row="2" Grid.Column="1" Margin="3" VerticalAlignment="Center" IsChecked="{Binding Closed, Mode=TwoWay}"/>
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Precision" Margin="3"/>
            <local:TextBoxDark Grid.Row="3" Grid.Column="1" Margin="3" Text="{Binding Precision, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
        </Grid>

        <Separator Margin="0,6"/>
        <TextBlock Margin="3" FontWeight="Bold" Text="Points"/>
        <DataGrid ItemsSource="{Binding Points}" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="None"
                  CanUserAddRows="False" CanUserDeleteRows="False" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="X" Width="*" Binding="{Binding X}"/>
                <DataGridTextColumn Header="Y" Width="*" Binding="{Binding Y}"/>
                <DataGridTextColumn Header="Speed" Width="*" Binding="{Binding Speed}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</local:DataUserControl>