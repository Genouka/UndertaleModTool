<Window x:Class="UndertaleModTool.ErrorDialog"
        IsVisibleChanged="Window_IsVisibleChanged"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UndertaleModTool"
        xmlns:loc="clr-namespace:UndertaleModTool.Localization"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}" Height="280" Width="520" MinHeight="180" MinWidth="360"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Grid Margin="16,14,16,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"
                       Foreground="#FFD32F2F" FontFamily="Segoe UI Symbol">&#x26A0;</TextBlock>
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="13"
                       VerticalAlignment="Center" MaxHeight="80"/>
        </StackPanel>

        <TextBox Grid.Row="1" Name="ErrorTextBox" IsReadOnly="True"
                 Text="{Binding ErrorText, Mode=OneWay}" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12"
                 Margin="0,0,0,10" Padding="6,4,6,4">
            <TextBox.ContextMenu>
                <local:ContextMenuDark>
                    <local:MenuItemDark Header="{loc:Loc Common_Copy}" Click="CopyMenuItem_Click"/>
                    <local:MenuItemDark Header="{loc:Loc Common_CopyAll}" Click="CopyAllMenuItem_Click"/>
                </local:ContextMenuDark>
            </TextBox.ContextMenu>
        </TextBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <local:ButtonDark Content="{loc:Loc ErrorDialog_CopyAll}" Margin="0,0,8,0"
                              Padding="12,4,12,4" Click="CopyAllButton_Click"/>
            <local:ButtonDark Content="{loc:Loc Common_OK}" IsDefault="True"
                              Padding="16,4,16,4" Click="OKButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
