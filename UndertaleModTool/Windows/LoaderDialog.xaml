<Window x:Class="UndertaleModTool.LoaderDialog"
        IsVisibleChanged="Window_IsVisibleChanged"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UndertaleModTool"
        mc:Ignorable="d"
        Title="{Binding MessageTitle}" Height="160" Width="440" MinHeight="120" MinWidth="320"
        WindowStartupLocation="CenterOwner" ResizeMode="CanMinimize"
        SizeToContent="Height">
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressState="Indeterminate"/>
    </Window.TaskbarItemInfo>
    <Grid Margin="20,18,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Message}" TextWrapping="Wrap"
                   FontSize="13" Margin="0,0,0,12"/>
        <ProgressBar Grid.Row="1" IsIndeterminate="True" Name="ProgressBar"
                     Height="20" Margin="0,0,0,8"/>
        <Grid Grid.Row="2" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusText}"
                       FontSize="11" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                       TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" Name="ProgressText" FontSize="11"
                       Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                       VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="3" Height="4"/>
    </Grid>
</Window>
